# copy to .env (gitignored). NEVER commit real keys.
# Same creds as clawd-video-chat / the harness — one clawd voice everywhere.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=Q4oILuo4P8VeXtE6FMLI   # Matthew Schmitz = clawd (clawd-video-chat .env on leftclaw). NOT the harness's Samantha.

# optional overrides
# ELEVEN_MODEL=eleven_flash_v2_5
# FFMPEG=/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg
