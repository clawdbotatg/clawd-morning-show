# copy to .env (gitignored). NEVER commit the real key.
OPENAI_API_KEY=sk-...

# optional overrides
# VOICE=ash                 # the brand voice (picked once from voice-samples.sh)
# TTS_MODEL=gpt-4o-mini-tts
# FFMPEG=/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg
